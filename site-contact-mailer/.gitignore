node_modules/
dist/
*.log

# Generated by `npm run sample` and `scripts/screenshot.js`.
sample/site-contacts-sample.xlsx
*.png

# ...but the app icon is source, not a screenshot. src/assets/icon.png in
# particular is loaded at runtime for the window icon, so it must be committed.
!build/icon.png
!src/assets/icon.png

# Never commit a real contact list.
*.xlsx
*.xls
*.csv
!sample/*.js
